

agent core runtime, se hostaa ne agentit


 agentcore gateway avulla voi exposaa omia api end pointteja. eli esim mul on risun-reseptit.fi/get 

 agentcore identity liittyy cognitoo ja autentikointiin




 iha ekana python virtual environment pystyyn projekti kohtasesti.

 python3.12 -m venv venv
 source venv/bin/activate
 ja sit strandi käyttöön:
pip install strands-agents // asentaa ton virtual envi sisää paketit
pip install strands-agents-tools // saa toolit käyttöön esim http_request

sit kato et terminaalis oikeudet käyttää claudee
aws bedrock list-foundation-models --region us-east-1 | grep claude-sonnet-4


python -u strands/hello-world.py // -u meinaa unbuffered. eli antaa striimatu vastaukse
python -u strands/strands-weather-agent.py
python -u strands/content-aggregator-agent.py




Agent Core Runtime - Hosts agents in containers (ECR)
Agent Core Gateway - API endpoints with custom domains
Agent Core Identity - Cognito authentication
MCP Servers - Tools/integrations for agents


